/*! handsome 2017-03-29 */
"use strict";
function getKey() {
  13 == event.keyCode && $("#soux").get(0).click();
}
function gotoTop(a, b) {
  (a = a || 0.1), (b = b || 10);
  var c = 0,
    d = 0,
    e = 0,
    f = 0,
    g = 0,
    h = 0;
  document.documentElement &&
    ((c = document.documentElement.scrollLeft || 0),
    (d = document.documentElement.scrollTop || 0)),
    document.body &&
      ((e = document.body.scrollLeft || 0), (f = document.body.scrollTop || 0));
  var g = window.scrollX || 0,
    h = window.scrollY || 0,
    i = Math.max(c, Math.max(e, g)),
    j = Math.max(d, Math.max(f, h)),
    k = 1 + a;
  if ((window.scrollTo(Math.floor(i / k), Math.floor(j / k)), i > 0 || j > 0)) {
    var l = "gotoTop(" + a + ", " + b + ")";
    window.setTimeout(l, b);
  }
}
$(".off-screen-toggle").click(function () {
  $("#aside").toggleClass("off-screen");
}),
  $(document).ready(function () {
    var a = document.getElementById("comment");
    $("#image-insert").bind("click", function () {
      var b = prompt("请输入图片地址", "http://");
      b &&
        (a.value +=
          '<img src="' +
          b +
          '" rel="external nofollow" class="b b-a wrapper-xs bg-white img-responsive pic-time">');
    }),
      $("#submit").click(function () {
        var a = $("#author").val(),
          b = $("#mail").val();
        return "" == $("#comment").val()
          ? (toastr.info("请填写内容"), $("#comment").focus(), !1)
          : "" == a
          ? (toastr.info("请填写昵称"), $("#author").focus(), !1)
          : "" == b
          ? (toastr.info("请填写邮箱"), $("#mail").focus(), !1)
          : ($("#submit").addClass("active"),
            $("#spin").addClass("show inline"),
            void $("#comment_form").submit());
      }),
      $("#login-submit").click(function () {
        var a = $("#navbar-login-user").val(),
          b = $("#navbar-login-password").val();
        return "" == a
          ? (toastr.info("请填写用户名"), $("#navbar-login-user").focus(), !1)
          : "" == b
          ? (toastr.info("请填写密码"), $("#navbar-login-password").focus(), !1)
          : ($("#login-submit").addClass("active"),
            void $("#spin-login").addClass("show inline"));
      }),
      $('[data-toggle="tooltip"]').tooltip();
  });
var bb = $("#soux").attr("href");
$("#soux").attr("href", ""),
  $("#keyword").bind("input propertychange", function () {
    var a = $("input[name=s]").val();
    $("#soux").attr("href", bb + a);
  }),
  $(document).ready(function () {
    $("#post-content img").each(function () {
      var a = $(this),
        b = (a.attr("title"), a.parent("a"));
      b.size() < 1 &&
        (b = a
          .wrap('<a href="' + this.getAttribute("src") + '"></a>')
          .parent("a")),
        b.addClass("light-link");
    }),
      $(".wrapper-lg").lightGallery({
        selector: ".light-link",
        mousewheel: !0,
      });
  }),
  console.log(
    "\n %c handsome-opensource v2.1.3 %c https://github.com/LemoFire/typecho-theme-handsome-opensource\n\n",
    "color:#444;background:#eee;padding:5px 0;",
    "color:#eee;background:#444;padding:5px 0;"
  );
